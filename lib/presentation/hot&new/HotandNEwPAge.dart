
import 'package:flutter/material.dart';

class hot_newPage extends StatelessWidget {
  const hot_newPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: const Center(child: Text('hot& new'),),);
  }
}